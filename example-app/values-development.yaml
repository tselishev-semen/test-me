services:
  - name: test-nginx
    namespace: rain-container-web
    image: nginx
    version: latest
    replicas: 2
    hosts:
      - localhost
    #TODO  we should be able to set the all ingress settings
    publicPort: 80
    ports:
      - port: 80
    envVars:
      - name: Name_Test
        value: Value_test